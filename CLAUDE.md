# CLAUDE.md

## 進め方

1. 目的、前提、完了条件を確認する。
2. 必要タスクを `TASKS.md` に落とす。
3. 実装、テスト、ハーネス、履歴更新を同じ作業単位で行う。
4. `npm.cmd run verify` と必要なUIスモークを実行する。
5. READMEと履歴に結果を残してからGitで管理する。

## レビュー観点

- 席替えロジックがUI状態に依存しすぎていないか。
- 児童データが外部送信されない前提を崩していないか。
- 配慮条件が「必ず保証」と誤読されない表現になっているか。
- 印刷、CSV、JSON、履歴が授業現場の実運用で破綻しないか。
- 公開前に `.env`、出力物、スクリーンショットなどが不用意に含まれないか。

## 主要ファイル

- `src/lib/seating.ts`: 座席生成、履歴比較、CSV処理。
- `src/App.tsx`: 先生向けワークベンチUI。
- `scripts/validate-project.mjs`: AI-DLCハーネス。
- `scripts/smoke-ui.mjs`: Playwright UIスモーク。
- `docs/AI_DLC.md`: 規約、スキル、フック、エージェント、安全確認。
